# labor-utils


```bash
sudo apt-get update
mkdir ${HOME}/project
cd ${HOME}/project
git clone https://github.com/IW276/labor-utils.git
cd labor-utils
chmod +x install_cuda.sh
./install_cuda.sh
source ${HOME}/.bashrc
chmod +x install_basics.sh 
./install_basics.sh
```
