# labor-utils


```bash
$ sudo apt-get update
$ mkdir ${HOME}/project
$ cd ${HOME}/project
$ git clone https://github.com/IW276/labor-utils.git
$ cd labor-utils
$ ./install_cuda.sh
$ source ${HOME}/.bashrc
$ ./install_basics.sh
```
